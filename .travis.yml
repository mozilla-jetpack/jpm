language: node_js
node_js:
  - "0.10"
  - "0.8"

addons:
  firefox: "28.0"

before_script:
  - pwd
  - uname -a
  - firefox --version
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"

script:
  - export JPM_FIREFOX_BINARY=`which firefox`
  - DISPLAY=:1 npm test
